# Volontairement sans règle.
#
# La redirection apex -> www ne peut PAS vivre ici : Workers n'accepte que des
# sources relatives dans _redirects (« Only relative URLs are allowed »), donc
# pas de règle qui discrimine sur l'hôte. Pages l'autorisait, plus Workers.
# Elle est faite au niveau de la zone, par une Redirect Rule Cloudflare, qui
# s'exécute de toute façon avant le Worker.
#
# Ne pas remettre `https://psyneupre.be/* ...` ici : le déploiement échoue.
